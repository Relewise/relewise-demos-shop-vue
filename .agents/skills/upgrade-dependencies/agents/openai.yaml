interface:
  display_name: "Upgrade Dependencies"
  short_description: "Safe npm upgrades with Trello-gated PR flow"
  default_prompt: "Run safe npm dependency upgrades from main for relewise-demos-shop-vue: verify a clean tree, sync main with fast-forward, require a Trello card URL (ask if missing), create chore/upgrade-dependencies-yyyyMM branch, discover package.json manifests, upgrade direct dependencies and devDependencies to latest stable, resolve manageable compatibility fallout, validate with npm run type-check, npm run build, and npm run test:unit -- --run, then push and open PR to main with Trello URL at the top of the PR description."
